{% extends "base.html.twig" %}
    
{% block title 'toto' %}

{% block body %}
<div class="jumbotron text-center">
<h1>Agence Lorem Ipsum</h1>
<p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptatem nulla laboriosam iste minus fuga asperiores non omnis tenetur ea a, quasi maiores maxime eaque rem aspernatur. Quia eius dignissimos esse.</p>
</div>
<div class="container">
    <h2>les derniers animes</h2>
    <div class="row flex">
        {% for anim in anims %}
            <div class="col-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            <a href="{{ path('anim.show', {id: anim.id, slug: anim.slug}) }}">{{ anim.title }}</a>
                        </h5>
                        <p class="card-text">
                            {{ anim.kanjiTitle }} ({{ anim.japTitle }})
                        </p>
                        <div class="text-primary" style="font-weight: bold;font-size:2rem;"> {{ anim.FormattedYear }}</div>
                    </div>           
                </div>
            </div>
        {% endfor %}
    </div>


</div>


{% endblock %}