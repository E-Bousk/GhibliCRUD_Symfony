
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
<div class="row">
    <div class="col-md-6">{{ form_row(form.title) }}</div>
    <div class="col-md-6">{{ form_row(form.kanjiTitle) }}</div>
</div>
<div class="row">
    <div class="col-md-4">{{ form_row(form.japTitle) }}</div>
    <div class="col-md-4">{{ form_row(form.director) }}</div>
    <div class="col-md-4">{{ form_row(form.year) }}</div>
</div>
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregister')}}</div>
{{ form_end(form) }}